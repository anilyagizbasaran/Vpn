<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is off everywhere by default. The two exceptions exist only so
  a debug build can reach a control plane running on the developer's machine:
    10.0.2.2  - the host, as seen from the Android emulator
    localhost - adb reverse tunnels

  Production traffic must go over HTTPS. Do not add your real API domain here.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
