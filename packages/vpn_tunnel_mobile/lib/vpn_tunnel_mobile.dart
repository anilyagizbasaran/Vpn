/// Android and iOS tunnel, backed by `wireguard_flutter_plus`.
///
/// Layer 2. The plugin import is confined here on purpose — the package has
/// been forked once already in this project's history, and the next fork
/// should not reach past this file.
library;

export 'src/mobile_tunnel.dart';
